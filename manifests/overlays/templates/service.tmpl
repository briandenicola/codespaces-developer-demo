apiVersion: v1
kind: Service
metadata:
  name: whatos
  namespace: whatos
  annotations:
    kubernetes.azure.com/ingress-host: ${APPLICATION_URI}
    kubernetes.azure.com/tls-cert-keyvault-uri: ${CERTIFICATE_KV_URI}
    kubernetes.azure.com/service-account-name: ${WORKLOAD_IDENTITY}
